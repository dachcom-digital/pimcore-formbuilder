# Using Presets

Define some Presets.

```yaml
form_builder:
    area:
        presets:
            -
            jobs:
                nice_name: 'Short Name'
                admin_description: 'Description'
                sites:
                    - 'domain1.com'
                    - 'domain2.com'

```