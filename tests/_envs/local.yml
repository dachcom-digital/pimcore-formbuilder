modules:
    config:
        \Dachcom\Codeception\Support\Helper\Browser\WebDriver:
            browser: chrome
            port: 4444
            restart: true
            wait: 1
            capabilities:
                chromeOptions:
                    args: ['--no-sandbox', '--disable-extensions', '--headless=new', '--remote-debugging-port=9222', '--disable-gpu', '--disable-dev-shm-usage', '--window-size=1280,1024']
