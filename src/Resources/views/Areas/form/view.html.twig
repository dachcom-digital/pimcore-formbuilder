{% extends '@FormBuilder/Form/default.html.twig' %}

{% block form_builder_form %}

    {% if form_template is not null %}

        {% form_theme form form_template %}

        {{ form_start(form) }}
            {{ form_widget(form) }}
        {{ form_end(form) }}

    {% endif %}

{% endblock form_builder_form %}